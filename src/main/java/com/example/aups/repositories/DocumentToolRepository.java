package com.example.aups.repositories;

import com.example.aups.models.DocumentTool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DocumentToolRepository extends JpaRepository<DocumentTool, Long> {
}
