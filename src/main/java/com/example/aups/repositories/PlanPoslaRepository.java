package com.example.aups.repositories;

import com.example.aups.models.PlanPosla;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanPoslaRepository extends JpaRepository<PlanPosla, Long> {
}
