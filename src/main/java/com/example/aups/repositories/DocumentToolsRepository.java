package com.example.aups.repositories;

import com.example.aups.models.DocumentTools;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DocumentToolsRepository extends JpaRepository<DocumentTools, Long> {
}
