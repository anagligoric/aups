package com.example.aups.repositories;

import com.example.aups.models.Nalog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NalogRepository extends JpaRepository<Nalog, Long> {
}
